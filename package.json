{
  "name": "nektome-freedom",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "release": "standard-version",
    "watch:chromium": "npx rimraf dist && node scripts/watch.js && node scripts/build-manifest.js chromium",
    "watch:firefox": "npx rimraf dist && node scripts/watch.js && node scripts/build-manifest.js firefox",
    "lint:js": "eslint .",
    "lint:js:fix": "eslint . --fix",
    "format": "prettier . --write",
    "build:chromium": "npx rimraf dist && npm run build:pages && npm run build:background && npm run build:content && node scripts/build-manifest.js chromium",
    "build:firefox": "npx rimraf dist && npm run build:pages && npm run build:background && npm run build:content && node scripts/build-manifest.js firefox",
    "build:pages": "vite build --config vite.config.pages.js",
    "build:background": "vite build --config vite.config.background.js",
    "build:content": "vite build --config vite.config.content.js"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@eslint-react/eslint-plugin": "^5.8.1",
    "@eslint/js": "^10.0.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^10.3.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "eslint-plugin-simple-import-sort": "^14.0.0",
    "globals": "^17.6.0",
    "prettier": "^3.9.6",
    "sass": "^1.102.0",
    "standard-version": "^9.5.0",
    "vite": "^8.0.12"
  }
}
